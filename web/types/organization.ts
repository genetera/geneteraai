export interface IOrganization {
  id: string;
  name: string;
  slug: string;
  organization_size: string;
  created_at: string;
  owner: string;
}

export interface IDocument {
  id: string;
  name: string;
  size: string;
}
